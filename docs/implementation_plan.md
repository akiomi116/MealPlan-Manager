# スマート献立マネージャー (Lightweight Plan) 開発提案書

## 1. プロジェクト概要
「在庫管理」の複雑さを排除し、「**撮って、決めて、買う**」までの体験をワンストップで提供するWebアプリケーション。
ユーザーは冷蔵庫の中身を管理・整理する必要がなく、**ただ写真を撮るだけ**で、今の状況に最適な1週間の献立と買い物リストが手に入ります。

## 2. コア機能 (統合フロー)

### 📸⚡ 1. Snap & Decide (即時提案フロー)
これは3つの機能（認識・献立・リスト）が統合された単一のワークフローです。

1.  **撮影 (Input)**: 冷蔵庫・キッチン周りを撮影（複数枚可）。
2.  **解析 (Analysis)**: 画像から食材を特定し、ユーザーの基本設定（人数・好み）および**特売情報**と組み合わせます。
3.  **提案 (Output)**: **プログレッシブ（段階的）表示**により、まず「検出された食材」を即座に表示。その裏で献立と買い物リストを生成し、完了次第表示します。

### 📲 1-B. QR Code Handoff (PC-スマホ連携)
PCユーザーのために、撮影だけをスマホで行うシームレスな連携機能を提供します。

1.  **QR表示**: PC画面に「スマホで撮影」ボタン → ユニークなセッションIDを含むQRコードを表示。
2.  **スマホ撮影**: スマホで読み取ると、ログイン不要の撮影専用ページが起動。
    - **チラシ/レシート対応**: ここで冷蔵庫だけでなく、**手元のチラシやレシート**も一緒に撮影可能にします。スクレイピングに頼らず特売情報をAIに直接読み取らせます。
3.  **リアルタイム同期**: スマホで写真を送ると、PC画面が自動的に切り替わり、解析フローがスタートします。

### 🗣️ 1-C. Voice Correction (音声修正)
AIの認識ミスや追加要望を、声で直感的に修正できます。
- **音声入力**: 「あと卵と牛乳も追加、人参は削除」と話しかけるだけで、Web Speech API等を用いて食材リストを自動修正します。

### 🛒 2. Smart Shopper (特売連携)
- 「クラシル」「クックパッド」等の外部情報を参照し、提案される献立に「今週の特売品」を積極的に組み込みます。
- 買い物リストには「買うべきもの（特売）」と「家にあるもの（撮影済み）」が明確に区別されます。

## 3. 技術スタック (変更なし)
- **Frontend**: Next.js 14+ (App Router), TypeScript, Tailwind CSS
- **Backend**: FastAPI (Python)
- **Database**: PostgreSQL (Prisma/SQLAlchemy)
- **AI**: Google Gemini 1.5 Flash/Pro (画像認識 & 献立生成 & リストアップを一度に処理検討)

## 4. システムアーキテクチャ (シンプル化)

```mermaid
graph TD
    User[ユーザー] -->|Photo Upload| FE[Frontend]
    FE -->|Images| BE[FastAPI Backend]
    
    subgraph "Stateless Processing"
        BE -->|1. Analyze Images| VisionAI[Gemini 1.5 Flash]
        BE -->|2. Fetch Bargains| Scraper[Bargain Search]
        VisionAI -->|Detected Items| Logic[Planning Engine]
        Scraper -->|On Sale Items| Logic
        
        Logic -->|Generate| Plan[One-Week Meal Plan]
        Logic -->|Generate| List[Shopping List]
    end
    
    BE -->|Save Result| DB[(PostgreSQL)]
    FE <--|Display Plan & List| BE
```

## 5. データベース設計 (軽量化)

永続的な「在庫」は持ちません。あくまで「その回の提案結果」を保存します。

- **Users**: ユーザー情報、好み設定
- **Sessions**: 1回の提案セッション（入力画像画像、生成日時）
- **GeneratedPlans**: 生成された献立（JSON形式で保存）
- **ShoppingLists**: 生成された買い物リスト

## 6. UI/UX コンセプト (Silver Friendly)
独居老人向けの「やさしい」インターフェースを目指します。

- **Big & Clear (大きく、はっきりと)**:
    - ボタンや文字サイズは通常の1.5倍〜2倍を基準とします。
    - コントラスト比を高め、視認性を最優先します（ダークモードより、温かみのある高コントラストなライトモード）。
- **Voice First (声で操作)**:
    - 入力だけでなく、結果も「音声読み上げ」で伝えます。
    - 「冷蔵庫の写真を撮ってね」など、AIが優しくガイドします。
- **Intuitive (直感的)**:
    - 専門用語を避け、「アルバムを見る」「献立を作る」など平易な言葉を使います。
    - 手順を一度に表示せず、一つずつ紙芝居のように進めるウィザード形式を採用します。

## 7. 将来的な拡張機能 (Future Scope)
今回の軽量化プランで意図的にスコープアウトしましたが、将来的に実装候補となる機能群です。

- **学習する献立 (Personalized Learning)**
    - 「この献立は好評だった/不評だった」という評価データの蓄積。
    - ユーザーの嗜好パターンを学習し、よりパーソナライズされた提案を行うアルゴリズムの強化。
- **学習する献立 (Personalized Learning)**
    - 「この献立は好評だった/不評だった」という評価データの蓄積。
    - ユーザーの嗜好パターンを学習し、よりパーソナライズされた提案を行うアルゴリズムの強化。
- **家計簿・栄養分析 (Analytics)**
    - 月間の食費集計と節約額の可視化。
    - 摂取栄養素の長期的なグラフ化と、健康改善アドバイス機能。
- **ユーザー認証 (Authentication)**
    - 複数デバイス同期のためのログイン機能（当面は端末保存で対応）。
